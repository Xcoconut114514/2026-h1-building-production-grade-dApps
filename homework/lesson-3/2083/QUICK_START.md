# 🚀 MiniSwap 快速启动指南

## 分钟级快速开始（推荐新手）

### 第1步：安装依赖（5分钟）

```bash
# 进入项目目录
cd C:\Users\28194\OneDrive\Desktop\plod

# 安装所有依赖
npm install
cd smart-contracts && npm install && cd ../frontend && npm install
```

### 第2步：启动Hardhat本地网络（1分钟）

打开新的PowerShell终端：
```bash
cd C:\Users\28194\OneDrive\Desktop\plod\smart-contracts
npx hardhat node
```

你会看到类似输出：
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/

Accounts:
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 (10000 ETH)
...
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb476c8c5f4a37e0e7f1b35cf3c6c
```

### 第3步：部署智能合约（2分钟）

打开第三个PowerShell终端：
```bash
cd C:\Users\28194\OneDrive\Desktop\plod\smart-contracts
npx hardhat run scripts/deploy.js --network localhost
```

记下输出的合约地址！

### 第4步：运行测试（3分钟）

在同一终端运行：
```bash
npm test
```

### 第5步：启动前端（1分钟）

打开第四个PowerShell终端：
```bash
cd C:\Users\28194\OneDrive\Desktop\plod\frontend
npm start
```

浏览器会自动打开 http://localhost:3000

### 第6步：在MetaMask中配置网络（5分钟）

1. 打开MetaMask钱包
2. 点击网络选择 → **添加网络**
3. 手动添加：
   - **网络名称**: Hardhat Local
   - **RPC URL**: http://127.0.0.1:8545
   - **Chain ID**: 1337
   - **货币符号**: ETH
4. 点击保存

5. **导入测试账户**:
   - MetaMask → 点击头像 → 导入账户
   - 粘贴第2步中的私钥
   - 钱包会显示10000 ETH

### 第7步：测试应用（2分钟）

1. 在前端点击"连接MetaMask钱包"
2. 切换到"流动性"标签
3. 输入：Token A = 100，Token B = 100
4. 点击"添加流动性"
5. MetaMask会弹出，点击"确认"
6. 等待交易确认
7. 切换到"交换"标签，输入数量交换

---

## 🎯 关键步骤检查清单

- [ ] 所有npm依赖已安装
- [ ] Hardhat节点运行在 http://127.0.0.1:8545
- [ ] 合约已部署（看到deployment.json）
- [ ] 测试全部通过（16/16）
- [ ] 前端启动在 http://localhost:3000
- [ ] MetaMask已配置本地网络
- [ ] MetaMask中有测试账户和10000 ETH

---

## 📋 故障排查

### npm命令不工作
```bash
# 检查Node.js版本
node --version  # 应该是v18+

# 如果没装Node.js，需要先安装
# 访问 https://nodejs.org/ 下载最新LTS版本
```

### Hardhat节点启动失败
```bash
# 清除缓存
cd smart-contracts
rm -r artifacts cache node_modules
npm install
npx hardhat node
```

### 前端无法连接合约
1. 检查 `frontend/src/components/SwapInterface.js`
2. 更新 `CONTRACT_ADDRESSES` 中的地址
3. 确保地址与 `deployment.json` 中的一致

### MetaMask提示"网络错误"
1. 确保Hardhat节点在运行
2. 重新启动节点
3. 在MetaMask中点击"断开连接"后重新连接

### 交易失败
- 检查账户余额是否足够
- 确认是否需要先授权代币
- 查看浏览器控制台错误信息

---

## 📊 理解项目架构

### 智能合约流程
```
Token.sol (ERC20)
    ↓
MiniSwap.sol (核心交换合约)
    ├── addLiquidity() - 用户添加流动性
    ├── removeLiquidity() - 用户移除流动性
    └── swap() - 用户交换代币
```

### 前端组件结构
```
App.js (主应用)
    ├── WalletConnect (钱包连接)
    ├── SwapInterface (交换界面)
    └── LiquidityInterface (流动性管理)
```

---

## 💡 核心概念

### 1. 流动性池
- 存储两种代币的储备金
- 交换时从池中取出代币

### 2. LP份额
- 流动性提供者的凭证
- 可以用来提取代币

### 3. 1:1交换
- 当前实现的简化模式
- 真实DEX使用AMM公式

### 4. 重入保护
- 防止攻击者重复调用函数

---

## 📈 下一步学习

### 进阶功能（自选）
1. **添加手续费机制**
   - 在swap()中收取0.3%手续费
   - 将手续费分配给LP

2. **实现LP挖矿**
   - 追踪LP的持有时间
   - 发放奖励代币

3. **改进价格模型**
   - 使用AMM公式 (x*y=k)
   - 而不是1:1固定比例

4. **多交易对**
   - 支持更多代币组合
   - 创建交易路由

---

## 📚 参考资源

- [Solidity 官方文档](https://docs.soliditylang.org/)
- [Hardhat 教程](https://hardhat.org/tutorial)
- [Uniswap V2 白皮书](https://uniswap.org/whitepaper.pdf)
- [Web3.js 文档](https://web3js.readthedocs.io/)

---

## ⏱️ 预计时间

| 步骤 | 时间 |
|------|------|
| 安装依赖 | 5分钟 |
| 启动节点 | 1分钟 |
| 部署合约 | 2分钟 |
| 运行测试 | 3分钟 |
| 启动前端 | 1分钟 |
| 配置MetaMask | 5分钟 |
| 测试应用 | 2分钟 |
| **总计** | **19分钟** |

---

**现在就开始吧！祝你学习愉快！🎉**
